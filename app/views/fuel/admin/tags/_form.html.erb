<%= form_for @tag, url: @url, html: { multipart: true } do |f| %>
  <% if @tag.errors.any? %>
    <div class="alert alert--warning">
      <p class="alert--content"><%= @tag.errors.full_messages.to_sentence %></h2></p>
      <%= link_to '#', class: 'alert--trigger' do %>
        <%= embedded_svg 'fuel/icons/alert-icon.svg', class: 'alert--close-icon' %>
      <% end %>
    </div>
  <% end %>
  <div class="form-container--left">
    <fieldset>
        <%= f.label :name %>
        <%= f.text_field :name %>
    </fieldset>
    <fieldset class="fieldset">
      <%= f.submit class: 'button button--secondary' %>
      <% if @tag.id %>
        <%= link_to 'Delete Tag', fuel.admin_tag_path(@tag), method: :delete, data: { confirm: "Are you sure you want to delete #{@tag.name}?" }, class: "delete-post button button--danger" %>
      <% end %>
    </fieldset>
  </div>
<% end %>