<%= form_for @faq_category, url: @url, html: { multipart: true } do |f| %>
  <% if @faq_category.errors.any? %>
    <div class="alert alert--warning">
      <p class="alert--content"><%= @faq_category.errors.full_messages.to_sentence %></h2></p>
      <%= link_to '#', class: 'alert--trigger' do %>
        <%= embedded_svg 'fuel/icons/alert-icon.svg', class: 'alert--close-icon' %>
      <% end %>
    </div>
  <% end %>
  <div class="form-container--left">
    <fieldset>
        <%= f.label :name %>
        <%= f.text_field :name %>
    </fieldset>
    <fieldset class="fieldset">
      <%= f.submit class: 'button button--secondary' %>
      <% if @faq_category.id %>
        <%= link_to 'Delete Category', fuel.admin_faq_category_path(@faq_category), method: :delete, data: { confirm: "Are you sure you want to delete #{@faq_category.name}?" }, class: "delete-post button button--danger" %>
      <% end %>
    </fieldset>
  </div>
<% end %>